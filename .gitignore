# Secrets -- .env holds OPENROUTER_API_KEY for the judge. Never commit it.
.env
# Staging copy of the jerry-agent request (will contain keys meant for that public branch).
jerry-agent-request.md

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Run outputs (checkpoints, logs)
runs/

# macOS
.DS_Store

# Local editor/agent state
.claude/settings.local.json

# tau2-bench sparse clone (pip does not ship data/; clone locally, do not vendor)
.deps/

# sandbox-runtime is a Node CLI; install via scripts/setup_tau2_sandbox.sh
node_modules/
