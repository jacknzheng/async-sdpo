[pytest]
# Repo root on sys.path so `train.*`, `data.*` and `run` import as packages from tests/.
pythonpath = .
markers =
    network: test requires network access (downloads the dataset or calls an API)
    tau2: requires the tau2 package (retail/airline/mock)
